# 간호기록 (NFH) ★ 최우선

> 환자가 구역(관찰/급성)에 들어오면 담당 간호사가 깊게 사정(assessment)하고,
> 그 결과로 **정형화된 간호기록을 자동 생성**하는 도구. 스무고개식 문진이 핵심.

**상태:** 🔴 **재설계 중** — 기존 "사정 툴 + 간호기록 툴"을 합치다 망가져 버그 多.
**처음부터 다시 만들기로 결정.** 현 단계는 요구사항 역정리.

## 📍 정본 (여기부터 보세요)

> 헷갈리면 이 표만 보면 됩니다. git 커밋 날짜는 `auto-save` 훅 때문에 최신 근거가 못 되니, **각 문서 상단 상태 마커가 진실원**입니다. 검증: `python3 .claude/skills/doc-status/status.py 3_서비스/NFH간호기록` (또는 `/doc-status`).

| 관심사 | 정본 문서 | 상태 |
|--------|-----------|------|
| 요구사항 (무엇을 왜) | `간호기록_요구사항정의서.md` (v4) | 🟢 정본 |
| 기능 명세 (단계별 입출력) | `간호기록_기능명세서_260613.md` | 🟢 정본 |
| 앱 (정본·배포) | `app/index.html` (+ `nfh_core.js`·`nfh_ui.js`·`nfh_style.css`) | 🟢 정본 (단계형+누락게이트+접이식 미리보기) |
| 흐름 배경 (간호사 관점) | `간호기록_화이트보드.md` | 🟡 보조 |
| UI/UX 가이드 | `사정앱_UIUX_가이드.md` | 🟡 보조 |
| 구 UI 시안 | `mockups/…/A_단계형_260620.html`(출처), `v4_차팅흐름_260618.html`, `…/B_한화면_260620.html` | ⚪ 구버전(superseded) |

→ **개발 = 요구사항정의서(v4) + 기능명세서 + `app/index.html`.** 데이터는 `_archive/nursing_data.json` 추출본(응급실 주호소)을 `nfh_core.js`에 인라인하며, 확장은 칩스 임상 큐레이션 작업으로 분리. `app/`은 현재 4파일(개발 편의)이며 핸드오프 전 단일 HTML로 합본 예정. 구버전(v1~v3·v4 목업, B 한화면, 실험 폴더 A)은 참고용.

## 목표 흐름
1. **환자 확인 팝업** (성함·팔찌 확인, 담당 소개, 금식 안내 — 멘트 대본 제공)
2. **도관 확인** (IV/주사바늘·콧줄·소변줄 — 타 병원에서 달고 올 수 있음)
3. **주호소 → 증상 카테고리 문진** (예: 복통 → 구역/구토·설사·열감·혈변·임신가능성·통증[부위·양상·점수·발병] 등 딱 필요한 10항목, 채팅형)
4. **사정 → 간호진단(예: 급성 통증) → 평가·중재**
5. **낙상 예방 교육**(디폴트, 누락 시 처벌 대상)
6. 예외: "약만 맞으러 온" 환자는 *이상 없음* 선택 시 건너뛰기

## 핵심 데이터 — "7만 항목" 정형 진술문
- 병원에서 실제 쓰는 정형 문구(예: "토했음"❌ → "오심 구토 있음"✅).
- 응급실+공통만 카테고리화하면 실제 문항은 **100~200개 이하**.
- ⚠️ **마스터 데이터가 아직 저장소에 없음(칩스 보유)** — 재구축의 최대 블로커.

## 다음 작업
- [x] 앞 플로우 ①맞이·확인(신원확인+도관+환의/귀금속)을 `app/`에 추가 (5단계) — 도관만 기록 포함, 신원/환의는 **칩스확인필요로 기록 미포함**
- [ ] 뒤 ⑧EMR 복붙(자동 붙여넣기) — 현재는 복사까지
- [ ] 도관 7종·환의/귀금속 진술문 **마스터 대조/칩스확인** → 확정되면 기록에 연결
- [ ] 데이터 확장 — 칩스 임상 큐레이션(주호소별 사정 묶음). 마스터(`Nr.statement_*.xlsx`)는 단어장일 뿐 매핑 없음
- [ ] 핸드오프 전 `app/`을 단일 HTML로 합본

## 구성
- `app/` — **정본 앱.** `index.html` + `nfh_data.js`(엑셀 빌드 산출물·데이터)·`nfh_core.js`(로직)·`nfh_ui.js`(렌더)·`nfh_style.css`. 단계형+누락게이트+접이식 미리보기. (합본 전까지 5파일)
- `data/` — **데이터 관리(엑셀→빌드).** `간호기록_원본_*.xlsx`(칩스 편집) → `build_nfh.py` → `app/nfh_data.js`. 상세 [`data/README_260621.md`](data/README_260621.md)
- `간호기록_요구사항정의서.md` — 요구사항 SSOT (**현재 v4**, 기능명세서·v4 목업 반영)
- `간호기록_기능명세서_260613.md` — 단계별 입출력·동작 명세
- `mockups/` — 구 UI 시안(전부 superseded). `AB_레이아웃테스트_260620/`(A 단계형=app 출처, B 한화면=참고), `v4_차팅흐름_260618.html`, `사정_플로우_UI.html`(보조). `v1~v3`·`사정앱_prototype`는 `mockups/_archive/`
- `사정앱_UIUX_가이드.md` / `.html` — UI/UX 가이드 (보조)
- `간호기록_화이트보드.md` / `.html` — 화이트보드 정리(`1_회의노트/간호기록.JPG`) (보조)
- `_archive/` — 구버전(`간호기록노트.html`), `nursing_data.json` 등
